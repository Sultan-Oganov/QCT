import { setComponent } from '@qctoken/register';
import { Typography } from './Typography';
import { TypographyName } from './types';
import config from './config.json';

setComponent(TypographyName, Typography, config);
